/**
 * puts2 - print even no.
 *
 * @str: string points for int.
 *
 * Return: even
 */

void puts2(char *str)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		if (i != 1, 3, 5, 9)
			putchar(str[i]);
	}
	putchar("\n");
}
