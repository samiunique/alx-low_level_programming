/**
 * @dest: destination of string to move.
 * @src: source of strin that move.
 *
 * Retun: concatenates
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	concatenate(s1, s2);

	return(0);
}
