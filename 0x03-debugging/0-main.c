#include "main.h"

/**
 * main - check the code.
 *
 * @i: int argument
 * Return: Always 0.
 */
int main(void)
{
    int i;

    i = 98 % 2;

    positive_or_negative(i);
    /*positive_or_negative(0);
    positive_or_negative(-1);
    */
    return (0);
}
