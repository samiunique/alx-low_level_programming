#include "main.h"

/**
 * mul - used to multiply integers
 *
 * @a: first input
 * @b: second input
 *
 * Return: multiple of a and b
 */
int mul(int a, int b)
{
    int x;

    x = a * b;

    return (x);
}
